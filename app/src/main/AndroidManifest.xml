<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tictactoe.nagiro.tictactoe4d" >
    <uses-sdk android:minSdkVersion="9"/>
    <!--las redes de anuncios moviles suelen requerir estos permisos con el fin de buscar contenidos:
     Interner y sobre la red
    -->
<uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@drawable/icono_app"
        android:theme="@android:style/Theme.Black.NoTitleBar"
        android:label="@string/app_name" >

        <activity
            android:name=".home"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".main"
            android:screenOrientation="portrait"
            ></activity>
        <activity
            android:name=".Interrogante"
            android:screenOrientation="portrait"
            ></activity>
        <activity
            android:name=".Interrogante_dos"
            android:screenOrientation="portrait"
            ></activity>
        <!--Ad network-specific activity package in the SDK-->
       <activity android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>
<meta-data android:name="com.google.android.gms.version"
    android:value="@integer/google_play_services_version"/>
    </application>

</manifest>
